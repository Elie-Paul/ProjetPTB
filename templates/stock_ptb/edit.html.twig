{% extends 'base.html.twig' %}

{% block title %}Edit StockPtb{% endblock %}
{% block javascripts %}
    <script>
    document.getElementById("thera").addEventListener("click", function() {
      console.log("Hello World!");
      let userid = document.getElementById('userid').innerText;
      let f="Commande";
      let param=userid+"+Le stock PTB a été modifié";
      //alert(param);
      let thera=new XMLHttpRequest();
      thera.onload=function ()
      {
        if(this.status == 200)
        {
          console.log("request ok");
          /*thera.open("POST",superLink+"/audit/altererstockptb",true);
          thera.send(param);*/
        }
      }
      if(f)
      {
        thera.open("POST",superLink+"/audit/altererstockptb",true);
        thera.send(param);
      }
    });
    </script>
{% endblock %}

{% block body %}
<div id="userid" style="display:none;">
    {{ app.user.id }}
 </div>
      <div class="box box-default">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('stock_ptb_lister') }}"><i class="fa fa-list"></i>&nbsp;<big>Liste des Stocks Billet Ptb</big></a>
          </div>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box box-solid box-primary">
            <div class="box-header">
                <h3 class="box-title">Modification du Stock</h3>   
            </div>
            <div class="box-body">
    {{ include('stock_ptb/_form.html.twig', {'button_label': 'Modifier'}) }}
         </div>
</div>
    {{ include('stock_ptb/_delete_form.html.twig') }}
{% endblock %}
