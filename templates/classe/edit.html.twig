{% extends 'base.html.twig' %}

{% block title %}Edit Classe{% endblock %}

{% block body %}
<div class="box box-default">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('classe_index') }}"><i class="fa fa-list"></i>&nbsp;Liste Classe</a>
          </div>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box box-solid box-primary">
    <div class="box-header">
        <h3 class="box-title">Modifier le libelle de la classe {{classe.libelle}}</h3>   
    </div>
    <div class="box-body">
        {{ include('classe/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>


    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
