{% extends 'base.html.twig' %}

{% block title %}Edit Guichet{% endblock %}

{% block body %}

   <div class="box box-primary">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4><i class="glyphicon glyphicon-plus"></i>&nbsp;<big>Modifier Guichet</big></h4>
          </div>
            <div class="col-md-6 text-right">
            <a class="btn btn-success pull-right" href="{{ path('guichet_index') }}"><i class="fa fa-list"></i>  Liste Guichet</a>
            </div>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box box-solid box-success">
    <div class="box-header">
        <h3 class="box-title">Modifier le guichet {{guichet.nom}}</h3>
    </div>
    <div class="box-body">
        {{ include('guichet/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>


    {{ include('guichet/_delete_form.html.twig') }}
{% endblock %}
