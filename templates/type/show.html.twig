{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
<div class="box box-default">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('type_index') }}"><i class="fa fa-list"></i>&nbsp; <big>Liste des types d'abonnés</big></a>&nbsp;
             <a class="btn btn-warning" role="button" href="{{ path('type_edit', {'id': type.id}) }}"><i class="glyphicon glyphicon-pencil"></i>&nbsp; <big>Modifier</big></a>
          </div>
          </div>
          <!-- /.box-body -->
    </div>

    <div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Information sur le type {{type.libelle}}</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type.libelle }}</td>
            </tr>
            <tr>
                <th>Section</th>
                <td>{{ type.section }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ type.prix }}</td>
            </tr>
        </tbody>
    </table>
    </div>
</div>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
