<form method="post" action="{{ path('commande_taxe_delete', {'id': commande_taxe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_taxe.id) }}">
    <button class="btn">Delete</button>
</form>
