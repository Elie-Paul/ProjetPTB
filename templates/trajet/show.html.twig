{% extends 'base.html.twig' %}

{% block title %}Trajet{% endblock %}

{% block body %}

<div class="box box-default">
          <div class="box-header with-border">
            <a class="btn btn-info" href="{{ path('trajet_index') }}">Retour</a>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box">
    <div class="box-header with-border">
        <h3 class="box-title">Information du trajet {{trajet.depart.libelle}}-{{trajet.arrivee.libelle}}</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>#</th>
                    <td>{{ trajet.id }}</td>
                </tr>
                <tr>
                    <th>Depart</th>
                    <td>{{trajet.depart.libelle}}</td>
                </tr>
                <tr>
                    <th>Arrivée</th>
                    <td>{{trajet.arrivee.libelle}}</td>
                </tr>
                <tr>
                    <th>Date de creation</th>
                    <td>{{ trajet.createdAt ? trajet.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Date de modification</th>
                    <td>{{ trajet.updatedAt ? trajet.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
    <a class="btn btn-info" role="button" href="{{ path('trajet_edit', {'id': trajet.id}) }}">modifier</a>


    {{ include('trajet/_delete_form.html.twig') }}
{% endblock %}
