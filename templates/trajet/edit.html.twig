{% extends 'base.html.twig' %}

{% block title %}Edit Trajet{% endblock %}

{% block body %}
<div class="box box-solid box-default">
    <div class="box-header">
        <h3 class="box-title">Modifier le trajet {{trajet.depart.libelle}}-{{trajet.arrivee.libelle}}</h3>   
    </div>
    <div class="box-body">
        {{ include('trajet/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
</div>
    

    <a class="btn btn-info" role="button" href="{{ path('trajet_index') }}">retour a la liste</a>


    {{ include('trajet/_delete_form.html.twig') }}
{% endblock %}
