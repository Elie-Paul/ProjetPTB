{% extends 'base.html.twig' %}

{% block title %}Ptb{% endblock %}

{% block body %}


<div class="box box-default">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('ptb_index') }}"><i class="fa fa-list"></i>&nbsp;Liste PTB</a>&nbsp;
            <a class="btn btn-warning" role="button" href="{{ path('ptb_edit', {'id': ptb.id}) }}"><i class="glyphicon glyphicon-pencil"></i>&nbsp;modifier</a>
          </div>
          </div>    
          <!-- /.box-body -->
        </div>
<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Information du train PTB</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>#</th>
                    <td>{{ ptb.id }}</td>
                </tr>
                <tr>
                    <th>Trajet</th>
                    <td>{{ ptb.trajet.depart.libelle }}-{{ ptb.trajet.arrivee.libelle }}</td>
                </tr>
                <tr>
                    <th>Section</th>
                    <td>{{ ptb.section.libelle }}</td>
                </tr>
                <tr>
                    <th>Date de création</th>
                    <td>{{ ptb.createdAt ? ptb.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Date de modification</th>
                    <td>{{ ptb.updatedAt ? ptb.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

    {{ include('ptb/_delete_form.html.twig') }}
{% endblock %}
