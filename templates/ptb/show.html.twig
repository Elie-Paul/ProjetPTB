{% extends 'base.html.twig' %}

{% block title %}Ptb{% endblock %}

{% block body %}
    <h1>Ptb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ptb.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ptb.createdAt ? ptb.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ ptb.updatedAt ? ptb.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ptb_index') }}">back to list</a>

    <a href="{{ path('ptb_edit', {'id': ptb.id}) }}">edit</a>

    {{ include('ptb/_delete_form.html.twig') }}
{% endblock %}
