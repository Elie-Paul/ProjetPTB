{% extends 'base.html.twig' %}

{% block title %}Edit Ptb{% endblock %}

{% block body %}

<div class="box box-default">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('ptb_index') }}"><i class="fa fa-list"></i>&nbsp;Liste des trains PTB</a>
          </div>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box box-solid box-primary">
    <div class="box-header">
        <h3 class="box-title">Modifier le train PTB</h3>   
    </div>
    <div class="box-body">
        {{ include('ptb/_form.html.twig', {'button_label': 'Modifer'}) }}
    </div>
</div>

    {{ include('ptb/_delete_form.html.twig') }}
{% endblock %}
