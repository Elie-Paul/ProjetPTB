{% extends 'base.html.twig' %}

{% block title %}BilletEvent{% endblock %}

{% block body %}
   <div class="box box-primary">
    <div class="box-header with-border">
            <div class="col-md-6">
            <h4><big> Information sur Billet d'Evenement</big></h4>
            </div>
            <div class="col-md-6 text-right">
            <a href="{{ path('billet_event_index') }}" class="btn btn-success"><i class="fa fa-list"></i> <big>Liste Billet Evenement</big></a>
            </div>
    </div>
</div>
<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Information du Billet {{billet_event.id}}</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Trajet</th>
                <td>{{ billet_event.trajet }}</td>
            </tr>
            <tr>
                <th>Guichet</th>
                <td>{{ billet_event.guichet.nom }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-warning" href="{{ path('billet_event_edit', {'id': billet_event.id}) }}">Modifier</a>
</div>
</div>
    

    {{ include('billet_event/_delete_form.html.twig') }}
{% endblock %}
