{% extends 'base.html.twig' %}
{% block title %}Edit Vignette{% endblock %}

{% block body %}

<div class="box-body">
    {{ form_start(form) }}
    <div class="row">
        <div class="col-xs-12">{{ form_row(form.imageFile) }}</div>        
    </div>
    <button class="btn btn-info">{{ button_label|default('Modifier') }}</button>
     {{ form_end(form) }}
</div>
{% endblock %}
{% block javascripts %}{% endblock %}
