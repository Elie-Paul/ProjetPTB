{% extends 'base.html.twig' %}

{% block title %}Edit Lieux{% endblock %}

{% block body %}
<h1><big> Gestion des stations/gares </big></h1>
<div class="box box-primary">
    <div class="box-header with-border">
            <div class="col-md-6">
            <h4><i class="fa fa-plus"></i> &nbsp;<big> Ajouter une nouvelle station/gare</big></h4>
            </div>
            <div class="col-md-6 text-right">
            <a href="{{ path('lieux_index') }}" class="btn btn-info"><i class="fa fa-list"></i> <big>Liste des Stations/Gares</big></a>
            </div>
    </div>
</div>

<!--div class="box box-primary">
          <div class="box-header with-border">
            <a class="btn btn-info" href="{{ path('lieux_index') }}">Retour</a>
          </div>
</div-->
<div class="box box-primary">
    <div class="box-header">
        <h3 class="box-title">Modifier la station {{lieux.libelle}}</h3>   
    </div>
    <div class="box-body">
        {{ include('lieux/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>


    {{ include('lieux/_delete_form.html.twig') }}
{% endblock %}
