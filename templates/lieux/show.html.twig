{% extends 'base.html.twig' %}

{% block title %}Lieux{% endblock %}

{% block body %}
<h1><big> Gestion des gares </big></h1>
<div class="box box-default">
    <div class="box-header with-border">
            <div class="col-md-6">
            <h4></h4>
            </div>
            <div class="col-md-6 text-right">
                <a href="{{ path('lieux_index') }}" class="btn btn-info"><i class="fa fa-list"></i><big> Liste des Gares</big></a>
                <a class="btn btn-warning" role="button" href="{{ path('lieux_edit', {'id': lieux.id}) }}"><i class="glyphicon glyphicon-pencil"></i> <big>Modifier</big></a>
            </div>
    </div>
</div>

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Information du lieux {{lieux.libelle}}</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>#</th>
                    <td>{{ lieux.id }}</td>
                </tr>
                <tr>
                    <th>Libelle</th>
                    <td>{{ lieux.libelle }}</td>
                </tr>
                <tr>
                    <th>Date de creation</th>
                    <td>{{ lieux.createdAt ? lieux.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Date de modification</th>
                    <td>{{ lieux.updatedAt ? lieux.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>


    <!--a class="btn btn-info" role="button" href="{{ path('lieux_index') }}"><i class="glyphicon glyphicon-chevron-left"></i>retour a la liste</a>

    <a class="btn btn-info" role="button" href="{{ path('lieux_edit', {'id': lieux.id}) }}"><i class="glyphicon glyphicon-pencil"></i>modifier</a-->


    {{ include('lieux/_delete_form.html.twig') }}
{% endblock %}
