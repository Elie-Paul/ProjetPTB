<form method="post" action="{{ path('commande_ptb_delete', {'id': commande_ptb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_ptb.id) }}">
    <button class="btn">Delete</button>
</form>
