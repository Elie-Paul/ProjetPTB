<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Impression</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
</head>
<Style>
    .page
    {
        display: grid;
        grid-template-columns: repeat(4, 190px);
        grid-template-rows: repeat(5, auto);
        -webkit-print-color-adjust: exact;
        width: 100%;
        
        overflow: auto;
        page-break-after: always;
        
    }
    @page {
    size: auto;   /* auto is the initial value */
    margin: 0px;  /* this affects the margin in the printer settings */
    }
    .billet
    {
        -webkit-print-color-adjust: exact;
        background-image: url('{{ asset('img/backBillet.png') }}');
        line-height:0.5px; 
        opacity: 0.85;
        margin-left: 10px;
        
        
    }
    
   .main
    {
        display: block;
        -webkit-print-color-adjust: exact;
       position: absolute;
        top: 0;
        left: 0;
        width :100%;
    }
  
   
</style>
<body style="width: 100%;">
    
    <div class="main" style="background-color:#{{color}};">
        {% set myVal = 0 %}
        {% for i in nbrebillet %}
            {% if myVal == 0 %}
            <div class="page">
            {% endif %}
            <div class="billet">
                <h3>N : {{i}}</h3>
                <h3>PTB/03/2019</h3>
                <h3>{{billet.guichet.nom }}</h3>
                <h5>Aller simple</h5>
                <h5>Départ:{{billet.ptb.trajet.depart.libelle}}</h5>
                <h5>Arrivée:{{billet.ptb.trajet.arrivee.libelle}}</h5>
                <h5>{{billet.ptb.section.libelle}}</h5>
                <h5>Tarif: {{billet.ptb.section.prix}}Fcfa</h5>
                <h3>***Senegal***</h3>
            </div>
            {% set myVal = myVal + 1 %}
            {% if myVal == 20 %}
                </div>
                {% set myVal = 0%}
            {% endif %}
        {% endfor %}
    </div>
    
</body>
</html>
<script>
    
    //var main = document.getElementById('main');
    /*main.style.backgroundColor="#"{{color}};*/
    window.print();
    window.onafterprint = 
    function(event) 
    { 
       document.location.href = "http://localhost:8000/billet/ptb/";

    };
</script>
