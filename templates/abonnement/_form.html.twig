{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    <a class="btn btn-primary" href="{{ path('type_index')}}">Créer un nouveau type</a>
{{ form_end(form) }}
<!--href="{{ path('type_index') }}"-->