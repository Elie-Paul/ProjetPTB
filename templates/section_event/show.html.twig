{% extends 'base.html.twig' %}

{% block title %}SectionEvent{% endblock %}

{% block body %}
    <h1>SectionEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section_event.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ section_event.libelle }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ section_event.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('section_event_index') }}">back to list</a>

    <a href="{{ path('section_event_edit', {'id': section_event.id}) }}">edit</a>

    {{ include('section_event/_delete_form.html.twig') }}
{% endblock %}
