<form method="post" action="{{ path('billet_navette_delete', {'id': billet_navette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billet_navette.id) }}">
    <button class="btn">Delete</button>
</form>
