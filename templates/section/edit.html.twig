{% extends 'base.html.twig' %}

{% block title %}Edit Section{% endblock %}

{% block body %}
<div class="box box-primary">
          <div class="box-header with-border">
          <div class="col-md-6">
            <h4></h4>
          </div>
          <div class="col-md-6 text-right">
            <a class="btn btn-info" href="{{ path('section_index') }}"><i class="fa fa-list"></i>&nbsp;Liste Section</a>
          </div>
          </div>
          <!-- /.box-body -->
        </div>
<div class="box box-solid box-success">
    <div class="box-header">
        <h3 class="box-title">Modifier la section {{section.libelle}}</h3>   
    </div>
    <div class="box-body">
        {{ include('section/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
</div>    

    {{ include('section/_delete_form.html.twig') }}
{% endblock %}
