{% extends 'base.html.twig' %}

{% block title %}TrajetEvent{% endblock %}

{% block body %}
    <h1>TrajetEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trajet_event.id }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ trajet_event.depart }}</td>
            </tr>
            <tr>
                <th>Arrivee</th>
                <td>{{ trajet_event.arrivee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trajet_event_index') }}">back to list</a>

    <a href="{{ path('trajet_event_edit', {'id': trajet_event.id}) }}">edit</a>

    {{ include('trajet_event/_delete_form.html.twig') }}
{% endblock %}
