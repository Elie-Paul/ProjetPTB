{% extends 'base.html.twig' %}

{% block title %}New TrajetEvent{% endblock %}

{% block body %}
    <h1>Create new TrajetEvent</h1>

    {% if error is defined %}
        <div class="panel panel-danger">
            {{ error }}
        </div>
    {% endif %}

    {{ include('trajet_event/_form.html.twig') }}

    <a href="{{ path('trajet_event_index') }}">back to list</a>
{% endblock %}
